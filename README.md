# Slipnet-Balanced-Client
Slipnet Balanced Client is a Windows GUI tool that scans DNS resolvers for slipnet.exe, finds the working/fast ones, and creates a balanced local SOCKS5/HTTP proxy pool for more stable routing.
